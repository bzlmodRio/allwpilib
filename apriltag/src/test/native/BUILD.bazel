load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "apriltag-cpp-test",
    size = "small",
    srcs = glob(["cpp/**/*.cpp"]),
    tags = [
        "no-asan",
    ],
    deps = [
        "//apriltag",
        "//thirdparty/catch2",
    ],
)

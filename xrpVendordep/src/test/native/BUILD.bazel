load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "xrp-cpp-test",
    size = "small",
    srcs = glob(["cpp/**"]),
    deps = [
        "//thirdparty/catch2",
        "//xrpVendordep",
    ],
)

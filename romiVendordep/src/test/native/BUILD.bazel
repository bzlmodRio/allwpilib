load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "romi-test",
    size = "small",
    srcs = glob(["cpp/**"]),
    deps = [
        "//romiVendordep",
        "//thirdparty/catch2",
    ],
)

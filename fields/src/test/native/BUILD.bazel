load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "fields-cpp-test",
    size = "small",
    srcs = glob(["cpp/**/*.cpp"]),
    deps = [
        "//fields/src/main/native:fields",
        "//thirdparty/catch2",
    ],
)

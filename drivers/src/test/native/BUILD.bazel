load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "drivers-cpp-test",
    size = "small",
    srcs = glob(["cpp/**/*.cpp"]),
    tags = [
        "no-tsan",
        "no-ubsan",
    ],
    deps = [
        "//drivers/src/main/native:drivers",
        "//thirdparty/catch2",
    ],
)

load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "glass-test",
    size = "small",
    srcs = glob(["cpp/**/*.cpp"]),
    tags = [
        "wpi-cpp-gui",
    ],
    deps = [
        "//glass",
        "//thirdparty/catch2",
    ],
)

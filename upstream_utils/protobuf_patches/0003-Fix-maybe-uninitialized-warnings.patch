From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Peter Johnson <johnson.peter@gmail.com>
Date: Sat, 10 Jun 2023 15:00:20 -0700
Subject: [PATCH 03/10] Fix maybe-uninitialized warnings

---
 src/google/protobuf/arena.cc                          | 4 ++--
 src/google/protobuf/generated_message_tctable_lite.cc | 6 +++---
 src/google/protobuf/io/coded_stream.cc                | 2 +-
 3 files changed, 6 insertions(+), 6 deletions(-)

diff --git a/src/google/protobuf/arena.cc b/src/google/protobuf/arena.cc
index 023e9c261d8b062397ef8239b28652c6a83e5c1c..a9cfb2ff4841a87de792aa53c6de3bce0d303cda 100644
--- a/src/google/protobuf/arena.cc
+++ b/src/google/protobuf/arena.cc
@@ -743,7 +743,7 @@ uint64_t ThreadSafeArena::Reset() {
 
 void* ThreadSafeArena::AllocateAlignedWithCleanup(size_t n, size_t align,
                                                   void (*destructor)(void*)) {
-  SerialArena* arena;
+  SerialArena* arena = nullptr;
   if (PROTOBUF_PREDICT_TRUE(GetSerialArenaFast(&arena))) {
     return arena->AllocateAlignedWithCleanup(n, align, destructor);
   } else {
@@ -756,7 +756,7 @@ void ThreadSafeArena::AddCleanup(void* elem, void (*cleanup)(void*)) {
 }
 
 SerialArena* ThreadSafeArena::GetSerialArena() {
-  SerialArena* arena;
+  SerialArena* arena = nullptr;
   if (PROTOBUF_PREDICT_FALSE(!GetSerialArenaFast(&arena))) {
     arena = GetSerialArenaFallback(kMaxCleanupNodeSize);
   }
diff --git a/src/google/protobuf/generated_message_tctable_lite.cc b/src/google/protobuf/generated_message_tctable_lite.cc
index eafa3da238e7e2f4ef4ef63c284638ff6a5c33e0..c8bf0136b3076ff80da278bacf4f30f7b1ea4ae5 100644
--- a/src/google/protobuf/generated_message_tctable_lite.cc
+++ b/src/google/protobuf/generated_message_tctable_lite.cc
@@ -1094,7 +1094,7 @@ PROTOBUF_ALWAYS_INLINE const char* TcParser::SingularEnum(
   PrefetchEnumData(xform_val, aux);
   const char* ptr2 = ptr;  // Save for unknown enum case
   ptr += sizeof(TagType);  // Consume tag
-  uint64_t tmp;
+  uint64_t tmp = 0;
   ptr = ParseVarint(ptr, &tmp);
   if (PROTOBUF_PREDICT_FALSE(ptr == nullptr)) {
     PROTOBUF_MUSTTAIL return Error(PROTOBUF_TC_PARAM_NO_DATA_PASS);
@@ -1139,7 +1139,7 @@ PROTOBUF_ALWAYS_INLINE const char* TcParser::RepeatedEnum(
   do {
     const char* ptr2 = ptr;  // save for unknown enum case
     ptr += sizeof(TagType);
-    uint64_t tmp;
+    uint64_t tmp = 0;
     ptr = ParseVarint(ptr, &tmp);
     if (PROTOBUF_PREDICT_FALSE(ptr == nullptr)) {
       PROTOBUF_MUSTTAIL return Error(PROTOBUF_TC_PARAM_NO_DATA_PASS);
@@ -1880,7 +1880,7 @@ PROTOBUF_NOINLINE const char* TcParser::MpVarint(PROTOBUF_TC_PARAM_DECL) {
 
   // Parse the value:
   const char* ptr2 = ptr;  // save for unknown enum case
-  uint64_t tmp;
+  uint64_t tmp = 0;
   ptr = ParseVarint(ptr, &tmp);
   if (ptr == nullptr) {
     PROTOBUF_MUSTTAIL return Error(PROTOBUF_TC_PARAM_NO_DATA_PASS);
diff --git a/src/google/protobuf/io/coded_stream.cc b/src/google/protobuf/io/coded_stream.cc
index 61f6393087d26a4b07c8ec51e9ff4064779f09e2..e819066ae3f21ba72bd041e3fe12d6e67cde5c65 100644
--- a/src/google/protobuf/io/coded_stream.cc
+++ b/src/google/protobuf/io/coded_stream.cc
@@ -494,7 +494,7 @@ int64_t CodedInputStream::ReadVarint32Fallback(uint32_t first_byte_or_zero) {
       (buffer_end_ > buffer_ && !(buffer_end_[-1] & 0x80))) {
     ABSL_DCHECK_NE(first_byte_or_zero, 0)
         << "Caller should provide us with *buffer_ when buffer is non-empty";
-    uint32_t temp;
+    uint32_t temp = 0;
     ::std::pair<bool, const uint8_t*> p =
         ReadVarint32FromArray(first_byte_or_zero, buffer_, &temp);
     if (!p.first) return -1;

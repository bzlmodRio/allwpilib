load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "telemetry-cpp-test",
    size = "small",
    srcs = glob(["cpp/**/*.cpp"]),
    tags = [
        "exclusive",
        "no-asan",
        "no-tsan",
    ],
    deps = [
        "//telemetry/src/main/native:telemetry",
        "//thirdparty/catch2",
    ],
)

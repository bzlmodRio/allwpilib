load("@rules_python//python:defs.bzl", "py_test")

# SOURCES = glob(["**/*Test.py"])
SOURCES = glob(["*Test.py"])
# SOURCES = glob(["**/Translation*", "**/Pose*", "**/Twist*", "**/Rotation*"])

[py_test(
    name = test_file[:-3],
    srcs = [test_file],
    deps = [
        "//wpimath/src/main/python:wpimath",
    ],
) for test_file in SOURCES]


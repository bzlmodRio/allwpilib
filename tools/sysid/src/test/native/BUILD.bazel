load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "sysid-test",
    size = "medium",
    srcs = glob(["cpp/**"]),
    tags = [
        "no-asan",
    ],
    deps = [
        "//thirdparty/catch2",
        "//tools/sysid/src/main/native:sysid-lib",
    ],
)
